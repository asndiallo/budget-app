node_modules
.next
.git
coverage
*.tsbuildinfo
next-env.d.ts
.DS_Store

# Personal financial data — never bake into an image layer
*.db
*.db-*
/import
/backups

# Secrets — passed in at runtime via env_file, not baked into the image
.env
.env.*
!.env.example

Dockerfile
docker-compose.yml
